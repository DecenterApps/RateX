export { allPoolsFunction, matchBothTokensFunction, matchOneTokenFunction }

import { Pool } from '../Pool'

function allPoolsFunction(first: number, skip: number): Pool[] {
    return [];
}

function matchBothTokensFunction(token1: string, token2:string): Pool[] {
    return [];
}

function matchOneTokenFunction(token: string): Pool[] {
    return [];
}
