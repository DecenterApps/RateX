{
  "scripts": {
    "postinstall": "cd UI && npm install && cd src/sdk && npm install && cd ../../../contracts && npm install",
    "start": "npm run --prefix ./UI start"
  },
  "dependencies": {
    "crypto": "^1.0.1",
    "prettier": "^3.0.1"
  }
}